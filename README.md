# Планировщик PWA

Минималистичное PWA-приложение для планирования задач, встреч и привычек с розовым дизайном.

## Возможности

- 📅 **Планирование задач** — на день, неделю, месяц и год
- ✅ **Трекер привычек** — отслеживание ежедневных привычек со статистикой
- 💰 **Финансовые цели** — накопления с прогрессом
- 📝 **Заметки** — быстрые записи с тегами и поиском
- 👥 **Встречи** — планирование событий с участниками
- 🔔 **Уведомления** — напоминания через Web Push API
- 📱 **PWA** — установка на устройство, работа офлайн

## Технологии

- React 18 + TypeScript
- Vite
- TailwindCSS
- Zustand (State Management)
- Dexie.js (IndexedDB)
- Framer Motion
- React Router v6
- Workbox (PWA)

## Установка

### 1. Установите Node.js

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nodejs npm

# Или через nvm (рекомендуется)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 20
nvm use 20
```

### 2. Установите зависимости

```bash
cd Planner
npm install
```

### 3. Запустите приложение

```bash
# Режим разработки
npm run dev

# Сборка для production
npm run build

# Предпросмотр production сборки
npm run preview
```

## PWA Иконки

Для полноценной работы PWA создайте иконки в папке `public/icons/`:
- icon-72x72.png
- icon-96x96.png
- icon-128x128.png
- icon-144x144.png
- icon-152x152.png
- icon-192x192.png
- icon-384x384.png
- icon-512x512.png

Используйте генератор: https://www.pwabuilder.com/imageGenerator

## Структура проекта

```
src/
├── components/       # React компоненты
│   ├── layout/       # Layout, Header, Sidebar, BottomNav
│   ├── ui/           # UI компоненты (Button, Card, Input...)
│   ├── tasks/        # Компоненты задач
│   ├── calendar/     # Календарные компоненты
│   ├── habits/       # Компоненты привычек
│   ├── finance/      # Компоненты финансов
│   ├── notes/        # Компоненты заметок
│   └── meetings/     # Компоненты встреч
├── pages/            # Страницы приложения
├── store/            # Zustand stores
├── services/         # IndexedDB, уведомления
├── hooks/            # Custom React hooks
├── types/            # TypeScript типы
├── utils/            # Утилиты
└── styles/           # CSS стили
```

## Деплой

### Web (PWA)

#### GitHub Pages

```bash
npm run build
# Загрузите содержимое папки dist на GitHub Pages
```

#### Vercel

```bash
npm i -g vercel
vercel
```

#### Netlify

```bash
npm run build
# Загрузите папку dist на Netlify
```

### Android APK

#### Автоматическая сборка через GitHub Actions

APK автоматически собирается при каждом push в `main`:

1. Перейдите на https://github.com/MarinaT2103/Planer/releases
2. Скачайте последний `app-release-signed.apk`
3. Установите на Android 12+ устройство
4. Введите PIN: **1590**

**Подробнее:** `.github/ACTIONS_README.md`

#### Ручная сборка

См. инструкции в `android-app/README.md`

## Лицензия

MIT
